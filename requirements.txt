# ── Core ──────────────────────────────────────────────────────
torch>=2.2.0
numpy>=1.26.0
pandas>=2.2.0

# ── HuggingFace Stack ─────────────────────────────────────────
transformers>=4.40.0          # Model loading, tokenizer
datasets>=2.19.0              # UltraFeedback loading
trl>=0.8.6                    # DPOTrainer, SFTTrainer
peft>=0.10.0                  # QLoRA / LoRA adapters
accelerate>=0.29.0            # Multi-device training abstraction
bitsandbytes>=0.43.0          # 4-bit quantization (QLoRA)
tokenizers>=0.19.0

# ── Evaluation ────────────────────────────────────────────────
openai>=1.0.0                 # GPT-4o as judge
scipy>=1.13.0                 # Statistical tests for win rate

# ── Experiment Tracking ───────────────────────────────────────
wandb>=0.17.0                 # Optional: track training runs

# ── Visualization ─────────────────────────────────────────────
matplotlib>=3.8.0
seaborn>=0.13.0

# ── Notebooks ─────────────────────────────────────────────────
jupyter>=1.0.0
ipykernel>=6.29.0
ipywidgets>=8.1.0

# ── Utilities ─────────────────────────────────────────────────
python-dotenv>=1.0.0          # Load .env for HF token
tqdm>=4.66.0
pyyaml>=6.0.1                 # Load yaml configs
huggingface-hub>=0.22.0       # Model/dataset downloads
